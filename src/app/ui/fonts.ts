import { Ubuntu, Ubuntu_Mono } from 'next/font/google';

export const ubuntu = Ubuntu({ subsets: ['latin'], weight: ['400', '700'], style: ['normal', 'italic'] });
export const ubuntu_mono = Ubuntu_Mono({ subsets: ['latin'], weight: ['400', '700'], style: ['normal', 'italic'] })